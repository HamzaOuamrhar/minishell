#include "libc.h"
#include "stdbool.h"
int main()
{
    // int fd[2];
    // pipe(fd);
    // int pid = fork();
    // if (pid == 0)
    // {
    //     close(fd[0]);
    //     int x;
    //     printf("put a number :");
    //     scanf("%d", &x);
    //     write(fd[1], &x, sizeof(int));
    //     close (fd[1]);
    // }
    // else
    // {
    //     close(fd[1]);
    //     int y;
    //     read(fd[0], &y, sizeof(int));
    //     y *= 3;
    //     printf("get from the child proccess %d", y);
    //     close (fd[0]);
    // }
    bool hello;
    if (!hello)
        puts ("yppp");
    else
        puts("nooo");
}